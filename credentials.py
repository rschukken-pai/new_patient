# Template for credentials.py -- this .example file IS safe to commit.
# It contains no real values, only placeholders.
#
# Copy it:
#   cp credentials.py.example credentials.py
# then fill in real values in credentials.py, which stays gitignored and
# gcloudignored -- it's the only file in this project that should ever
# contain them.

GCP_PROJECT = "pai-production-395410"
GCP_DATASET = "PAP"
API_KEY = "lZh8FCgq59xnaL6yT7neJx0VoopxAtDcl1z4ngIldsk="